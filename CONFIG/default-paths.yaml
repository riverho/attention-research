# Delivery config read by the agent at digest-delivery time.
# All other paths are resolved by the scripts directly; they are not
# read from this file.

delivery:
  default_channel: "telegram"
  telegram:
    chat_id: ""
  whatsapp:
    recipient: ""

config_version: "1.0.0"
